CREATE DATABASE student_portal;
USE student_portal;
CREATE TABLE students (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(50), course VARCHAR(50));
INSERT INTO students VALUES (NULL, 'Alice', 'BCA');
INSERT INTO students VALUES (NULL, 'Rahul', 'BSc IT');
INSERT INTO students VALUES (NULL, 'Sneha', 'BBA');
SELECT * FROM students;
DELETE FROM students WHERE id=2;
DROP TABLE students;
